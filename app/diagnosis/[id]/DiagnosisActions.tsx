"use client";
import { Button, Stack } from "@mui/material"; import DeleteIcon from "@mui/icons-material/Delete"; import EditIcon from "@mui/icons-material/Edit"; import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react";
export function DiagnosisActions({ id }: { id: string }) { const router = useRouter(); const [deleting, setDeleting] = useState(false); async function remove() { if (!confirm("この自分探しの結果を削除します。よろしいですか？")) return; setDeleting(true); const r = await fetch(`/api/diagnosis/${id}`, { method: "DELETE" }); if (r.ok) router.push("/diagnosis/history"); else { const d = await r.json(); alert(d.error || "削除に失敗しました。"); setDeleting(false); } } return <Stack className="diagnosis-result-actions" direction={{ xs: "column", sm: "row" }} spacing={2} sx={{ mt: 3 }}><Button component={Link} href={`/diagnosis/${id}/edit`} variant="outlined" startIcon={<EditIcon />}>記録を編集</Button><Button color="error" variant="outlined" disabled={deleting} onClick={remove} startIcon={<DeleteIcon />}>削除</Button><Button component={Link} href="/diagnosis/history">履歴へ戻る</Button></Stack>; }
