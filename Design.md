# AI自分探しサービス デザインガイド

## 1. デザインコンセプト

**「自分の内側にある光を、静かな聖域で見つける」**

LPトップ画面のクリスタルの聖域を、サービス全体のビジュアル言語とする。神秘的でありながら怪しくならないこと、上質でありながら操作を妨げないことを重視する。

- LPは世界観を伝える「外の聖域」。背景画像、光、きらめき、金の装飾を豊かに使う。
- 認証・会員・法務画面は集中して操作する「内の聖域」。同じ色・書体・質感を、静かな背景とガラスカードへ整理して使う。
- 重要な操作はクリスタルブルー、特別感や小さな強調はゴールドで示す。

## 2. デザイントークン

| 用途 | 値 | 使い方 |
| --- | --- | --- |
| Deep navy | `#071B49` | ヘッダー、フッター、濃色面 |
| Sanctuary navy | `#102C68` | ブランドの基準色 |
| Crystal blue | `#294B9A` | 主要ボタン、リンク、フォーカス |
| Sky blue | `#DCE9FB` | 全画面の基礎背景 |
| Gold | `#D8A836` | 罫線、アイコン、アイブロウ |
| Pale gold | `#F3D788` | 濃色面上の強調 |
| Ink | `#10275E` | 本文と見出し |
| Glass | `rgba(247,251,255,.82)` | カード、ナビゲーション面 |

角丸はカード `24px`、入力 `14px`、ボタンはピル型。影は青みを帯びた柔らかなものにし、真っ黒な影は使わない。

## 3. タイポグラフィ

- ブランド名・ページタイトル・大見出し: `Yu Mincho`, `Hiragino Mincho ProN`, serif
- 本文・UI・入力: `Noto Sans JP`, `Yu Gothic`, sans-serif
- 見出しは文字間を少し広げ、余白を十分に取る。
- 本文は可読性を優先し、行間 `1.8–1.9` を基準とする。

## 4. 共通レイアウト

- 全ページに共通のブランドヘッダーとフッターを置く。
- 内部画面の本文幅はフォーム `640px`、情報画面 `900px` を上限とする。
- ページ背景は淡い青のグラデーションと控えめな光彩で構成する。
- 主カードは半透明の白、金の細い上辺、内側の白いハイライトで「光を含むガラス」を表す。
- ページ先頭に英字のアイブロウ、和文タイトル、短い説明の順で置く。

## 5. コンポーネント

### ボタン

- Primary: 青のグラデーション、白文字、控えめな発光。
- Secondary: 白〜淡青の面、青い枠と文字。
- 危険操作を除き、ゴールド単色を主要ボタンには使わない。
- キーボードフォーカスは淡い青の3pxアウトラインで明示する。
- ボタンラベルは常に横書きの1行表示とし、文字単位の折り返しや縦書き表示を発生させない。
- 表示幅が不足する場合はボタンを過度に縮小・縦長化せず、段組みまたは1列表示へ切り替える。
- ボタンの高さは原則 `44–52px` に保ち、幅の変化によって高さや形状が意図せず変わらないようにする。

### カード

- `24px` の角丸、薄い青の境界線、青いソフトシャドウ。
- 情報のまとまりごとに余白を取り、カードの入れ子を増やしすぎない。

### フォーム

- ラベルを常に明確にし、背景は半透明の白。
- 無効状態もコントラストを確保する。
- 成功・失敗は色だけに依存せず、文章で結果を伝える。

### 待機状態

- 画面遷移、認証、データ取得、保存など、即時完了しない処理では全画面共通の待機表示を使う。
- マウス環境では標準の `wait` カーソルに加え、青白い水晶と金色の光輪からなる追従ポインターを表示する。
- 待機中は二重送信を防ぐ透明な操作レイヤーを置き、支援技術には `role="status"` とライブリージョンで「処理中」を通知する。
- タッチ端末では追従ポインターを表示せず、各ボタン内の進捗表示を利用する。
- `prefers-reduced-motion` では水晶と光輪の回転・脈動を停止する。

## 6. モーションとアクセシビリティ

- LP以外では常時動く装飾を避け、画面表示時の短いフェード程度に留める。
- `prefers-reduced-motion` ではアニメーションを実質停止する。
- 本文コントラスト、44px以上の操作領域、スキップリンク、見出し階層を維持する。
- 装飾画像には空の代替テキストを用い、意味のある画像だけ説明する。

## 7. 適用範囲

このガイドは `/`、`/auth/signin`、`/dashboard`、`/profile`、`/legal` と今後追加する全画面に適用する。新規画面は共通の `InteriorShell` とMUIテーマを起点に構築する。

## 8. 会員ダッシュボード

- 会員カードにはアバター、名前、メール、プラン、性別、年代、職業をまとめて表示する。
- プランはチップ形式で表示し、無料プランはクリスタルブルー、有料プランはゴールドで視覚的に区別する。
- 基本プロフィールは3列の情報カードとし、スマートフォンでは1列に組み替える。
- 自由入力がある場合は、金色の縦罫線を持つ静かな引用面として表示する。
- プロフィール編集では画像プレビュー、選択項目、自由入力、保存操作を一つの流れで完結させる。
- プランは契約情報であるためプロフィール画面では編集させず、将来の決済・契約管理機能からのみ変更する。
- 操作ボタンはPCでは主要操作を広く、補助操作を均等に並べる。中間幅では主要操作を1段目いっぱい、補助操作を2段目の2列、スマートフォンではすべて1列に組み替える。

## 会員限定AIキャリア診断

- `/diagnosis` 以下は認証済み会員のみ利用でき、未認証時は会員ログインへ遷移する。
- 5問の自由回答をClaude Haiku 4.5で分析し、診断タイトル、概要、強み、推奨キャリア、助言、短期・中期・長期ロードマップを保存する。
- 診断結果はMongoDBの `diagnoses` コレクションにユーザーIDと紐づけ、本人のデータのみ作成・閲覧・更新・削除できる。
- 画面はLP・ダッシュボードと同じ青、水晶、金の世界観を共通のInteriorShellで継承する。
- 診断フォーム、直近結果、履歴一覧、詳細、編集画面を設ける。削除時は確認ダイアログを表示する。
